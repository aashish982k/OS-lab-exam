The code is a simple python code which is used to swap the 2 numbers and display the output of it.


To run the code 
* Install python and IDE (like VS code)
* Open the File
* Run the code by typing "python3 hello.py" inside the VS code terminal
